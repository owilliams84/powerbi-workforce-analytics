# Power BI Desktop writes these next to the PBIP while a project is open. They are per-machine
# state (window layout, cached model data, credentials), not part of the definition.
.pbi/
**/.pbi/
localSettings.json
**/localSettings.json
*.pbix
cache.abf
diagramLayout.json

# Working captures from the screenshot loop; only the cropped pages are committed.
screenshots/raw_*.png
screenshots/_*.png

# Python
__pycache__/
*.py[cod]

# OS
Thumbs.db
desktop.ini
.DS_Store

# The DAX-vs-pandas comparison output; regenerated against a live model.
etl/checks/dax_actual.csv
